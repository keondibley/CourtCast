import pandas as pd
import matplotlib as plt
games_data= pd.read_csv("games.csv")
headers = list(games_data.columns)
games_data = games_data.loc[games_data.SEASON >= 2015]
games_data.sort_values(by=["GAME_DATE_EST"])        
games_data = games_data.reset_index()
print(games_data)

games.isnull().sum() # 99 null values for some variables

(games.isnull().sum()/(len(games)))*100 # very low percentage of null values


print(games.GAME_ID.nunique()) # 26622
print(len(games)) #26651, there are 29 duplicate values
